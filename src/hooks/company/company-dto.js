export class CompanyPostDTO {
  constructor({ name, type }) {
    this.name = name;
    this.type = type;
  }
}

export class CompanyPatchDTO {
  constructor({ name, type }) {
    this.name = name;
    this.type = type;
  }
}
